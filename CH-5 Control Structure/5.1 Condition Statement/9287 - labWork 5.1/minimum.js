// Write a Program to find the minimum number from the given 2 numbers using if else

let fnum = prompt("Enter first number :");
snum = prompt("Enter second number :");

if (fnum < snum) {
    document.write(fnum, " is the Minimum number");
}
else {
    document.write(snum, " is the Minimum number");
}

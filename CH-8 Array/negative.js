document.write("<h3>Let's do negative array Programm..</h3>");

const numbers = [-21, -14, 15, 26, 34, -40, -54];

for (const element of numbers) {
    if (element < 0) {
        console.log(element);
    }
}
